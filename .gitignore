node_modules/
out/
worksheets/
*.log
.DS_Store
.ralph/
prd.json
progress.txt
# 로컬 툴링 상태(OMC/fablize) — 추적하지 않음
.omc/
.fablize/
.playwright-mcp/

# 오케스트레이터 임시 산출물(감사·재실행용 로컬 보존, 추적 안 함)
_workspace/
_workspace_prev/

# 스크래치패드(로컬 작업 공간·검증 증거 PNG/PDF) — 추적 안 함
# (기존에 큐레이션되어 이미 추적 중인 scratchpad 파일은 그대로 유지된다)
scratchpad/
# 임시 검증 스크린샷(루트)
/verify-*.png
/verify-*.jpeg
