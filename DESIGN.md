# Worksheet Grab Editor Design System

이 문서는 현재 편집기 UI를 재디자인하기 위한 제안서가 아니라, 이미 사용 중인 시각·상호작용 규칙을 코드와 같은 수준의 계약으로 기록한다. 활동지 종이 캔버스가 항상 주인공이며 편집기 크롬은 교사의 작업을 방해하지 않는 조용한 도구여야 한다.

## 1. Atmosphere

- 차분하고 실용적인 교사용 제작 도구
- 회색 작업대 위 흰 종이 캔버스를 중심으로 구성
- 장식보다 상태 구분, 빠른 탐색, 인쇄 결과 예측을 우선
- Canva·Google Slides에 익숙한 사용자가 페이지 목록, 선택, 재배치 동작을 즉시 이해할 수 있어야 함

## 2. Color

현재 CSS는 custom property 없이 아래 값을 직접 사용한다. Phase 2에서는 색 체계를 바꾸지 않는다.

| 용도 | 값 |
| --- | --- |
| 앱/본문 기본 글자 | `#24292f` |
| 보조 글자 | `#57606a` |
| 기본 경계 | `#d0d7de` |
| 약한 경계/hover 배경 | `#eaeef2` |
| 패널 배경 | `#fbfbfd` |
| 작업대 배경 | `#e8eaed` |
| 종이/입력 배경 | `#ffffff` |
| 선택·주요 액션 | `#2563eb` |
| 선택 글자 | `#1d4ed8` |
| 선택 ring | `#93c5fd` |
| 약한 선택 배경 | `#dbeafe`, `#f0f6ff` |
| 성공 | `#1a7f37` / `#dafbe1` |
| 경고 | `#7d4e00` / `#fff8c5` |
| 오류 | `#cf222e` / `#ffebe9` |

## 3. Typography

- 편집기 기본 글꼴은 `Pretendard`, `"Malgun Gothic"`, sans-serif 계열을 사용한다.
- 편집기 크롬은 주로 `11px`에서 `13.5px` 범위다.
- 탭과 주요 패널 동작은 `12px`에서 `12.5px`, 패널 제목은 `13.5px`를 사용한다.
- 활동지 내부 서체와 크기는 문서·테마 데이터의 소유이며 편집기 크롬이 덮어쓰지 않는다.
- 선택 상태는 색뿐 아니라 굵기, 경계, ring을 함께 사용한다.

## 4. Spacing and Layout

- 기본 간격 단위는 `4px`이며 실제 컴포넌트는 주로 `4`, `6`, `8`, `10`, `12`, `16`, `20px`를 사용한다.
- 워크스페이스는 `220px / minmax(0, 1fr) / 268px` 3열 구조다.
- 좌측 패널은 페이지·삽입·내 블록을 소유하고, 중앙은 종이 캔버스, 우측은 인스펙터를 소유한다.
- 페이지 썸네일 가시 폭은 `148px`다. 원본 페이지 비율을 유지하며 크기를 알 수 없으면 A4 96dpi인 `794 × 1123`을 사용한다.
- 캔버스는 상단 `20px`, 하단 `60px` 여백을 두고 스크롤한다.

## 5. Components and States

### App Bar

- 문서 제목, 검수, 미리보기, 저장과 전역 상태를 제공한다.
- 캔버스 편집과 경쟁하는 강한 색 면적을 추가하지 않는다.

### Left Tabs

- 기본, hover, active, disabled 상태를 구분한다.
- active 탭은 파란 글자, 굵은 글자, 하단 파란 경계로 표시한다.
- 비활성 패널은 시각적으로만 가리는 것이 아니라 `hidden` 상태로 상호작용 대상에서 제외한다.

### Page Thumbnail

- 구조: 페이지 미리보기, 페이지 번호, 선택적 역할 컨트롤.
- 상태: default, hover, focus-visible, active, dragging, drop-target, disabled.
- active는 파란 경계와 밝은 파란 ring으로 표시하고 `aria-current="page"`를 함께 사용한다.
- focus-visible은 active 여부와 무관하게 키보드 초점이 보여야 한다.
- dragging은 원본을 흐리게 표시하고, drop-target은 삽입 위치를 선으로 보여준다.
- 썸네일 미리보기 iframe은 포인터 입력을 받지 않는다.

### Inspector and Popups

- 입력은 흰 배경, 중립 경계, `6px` radius를 사용한다.
- 메뉴와 팝업만 제한적으로 shadow를 사용한다.
- 페이지 문맥 메뉴와 키보드 명령은 동일한 command 경계를 호출해야 한다.

### Paper Canvas

- 종이 렌더가 시각적 진실 원천이다.
- Page Shell 자체를 contenteditable로 만들지 않는다.
- 개체 선택·편집 표시는 종이 내용과 구분되는 편집기 전용 decoration이다.

## 6. Motion

- 현재 캔버스 zoom 전환은 `100ms ease-out`이다.
- 페이지 재배치는 포인터 이동 중 즉시 위치 피드백을 주되, 놓기 전에는 문서 모델을 변경하지 않는다.
- 재배치 완료 후 별도 장식 애니메이션을 추가하지 않는다.
- `prefers-reduced-motion` 환경에서는 동작 이해에 필요하지 않은 전환을 생략한다.

## 7. Depth

- 기본 패널과 썸네일은 경계로 층위를 구분한다.
- active 썸네일은 `2px` ring을 사용한다.
- 메뉴·드롭다운·모달 같은 일시적 overlay에만 shadow를 사용한다.
- 종이 자체의 깊이 표현은 문서 렌더 스타일을 따르며 썸네일에서는 제거한다.

## 8. Accessibility Contract

- 목표 기준은 WCAG 2.2 AA다.
- 모든 페이지 썸네일은 키보드로 초점 이동·선택할 수 있어야 한다.
- `Alt+ArrowUp`과 `Alt+ArrowDown`은 초점 페이지를 한 칸 재배치하고 기본 동작을 막는다.
- 재배치 뒤 초점과 현재 페이지 ID를 유지한다.
- 페이지 목록은 list semantics를 유지하고, 현재 페이지는 `aria-current="page"`로 알린다.
- 역할 선택 컨트롤은 페이지 번호가 포함된 이름을 제공한다.
- drag는 유일한 재배치 수단이 아니며 키보드와 문맥 메뉴가 같은 기능을 제공한다.
- 색만으로 active, focus, drop 위치를 전달하지 않는다.

현재 hardcoded 색·간격 값은 기존 기술 부채다. 이 리팩터링은 CSS token 전환이나 전면 반응형 재설계를 포함하지 않는다.
