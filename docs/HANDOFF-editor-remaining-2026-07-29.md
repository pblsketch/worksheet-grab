# 핸드오프 — 편집기 잔여 결함 작업 (2026-07-29)

이 문서 하나로 새 세션이 이어받을 수 있게 쓴다. **계획의 단일 진실은
`docs/PLAN-editor-remaining-2026-07-29.md`** 이고, 이 문서는 "지금 어디까지 왔고 무엇을 조심하라"다.

---

## 1. 지금 상태

| 항목 | 값 |
|---|---|
| main | 0단계·1단계·2단계 ①·**3단계 3a** 병합 완료 (병합 대기 없음) |
| 작업 브랜치 | `feat/editor-ux-qa` — main 과 동기 |
| 워크트리 | `E:/github/worksheet-grab-editorqa` (같은 저장소, 다른 폴더) |
| 단위 | **752** (pass 743 · skip 9 · **fail 0**) |
| 렌더 | **147** (pass 145 · skip 1 · **todo 1** · **fail 0**) — todo 는 미수정 D14(숨기지 않고 표시) |
| 병행 세션 | **없음**(옆 세션 종료 확인). 착수 전 `git status --porcelain` 는 여전히 확인할 것 |

**첫 할 일**: 계획 **3단계 3b** — `refreshDerived({level})` 수렴 · ⑦(타이핑 뒤 칩·썸네일 갱신) ·
C16(`runReflow` 의 `updateAll`/`refreshLayers` 누락) · 검수 디바운스 중 칩 중립 표시.
3a(⑨·⑧)는 끝났다. 대기 중인 병합은 없다.

> **3b 착수 전 알아 둘 것**: `leftPanel.renderThumbs` 는 시트마다 스타일 수집 + 복제라 **비싸다** —
> 타이핑마다 부르면 안 되고 디바운스가 필요하다. 현재 타이핑 경로에는 `scheduleReflow()`(300ms)만
> 걸려 있는데 리플로우는 `changed` 일 때만 파생 뷰를 갱신하므로, **페이지 재배정이 없는 편집에서는
> 칩·썸네일이 영영 안 갱신된다** — 그게 ⑦의 실체다.

> **렌더 플레이크 1건 관측(2026-07-29)**: 전량 1회차에서 `editor-export.render.test.js` 가 파일 단위로
> 실패했으나 **단독 재실행 2/2 초록 · 전량 2회차 fail 0** 이었다. 개별 테스트 ✖ 없이 파일 단위로만
> 찍혀 원인은 미상이다(상세를 필터가 삼켰다). 다시 보이면 **필터 없이** 전량 로그를 남길 것.

---

## 2. 이번 사이클에 끝낸 것

**결함 12건 수정·병합**(대장 `docs/DEFECTS-editor-2026-07-28.md` 에 원인·재현·수정 전부 기재)

- D1 **P0** 편집 중 Backspace 가 표·학습목표 박스를 통째로 삭제
- D5 **P0** 저장 왕복 중 입력한 편집 유실
- D2 크기 손잡이가 마퀴를 동시 발동해 선택 해제 · D3 드래그 후 클릭 1회 소실 · D4 무동작 배치 전환 버튼
- D6 배지 더블클릭 시 글자 실종 · D7 세로 확대가 실제 테두리에 안 닿음 (사용자 보고)
- D8 프리셋이 ⠿·리사이즈 손잡이를 인쇄물에 굳힘 · D9 병합셀 재병합 시 표 구조 붕괴 · D10 타이핑+명령 undo 병합
- **0단계** 저장 실패 시 용지·테마·PNG·PDF 진행 차단 (`3a8cf6d`)
- **1단계 ⑥** 모드 토글 세대 가드 (`1079e20`, 병합 대기)

**0.5단계 프로브(중요 — 다음 단계 설계를 바꾼 사실)**
`execCommand('insertHTML')` 은 `input` 을 **정확히 1회 발화한다**(실측). 따라서
`tableEdit.js:221`·`partEdit.js:124` 의 기존 `input` 리스너가 되읽기를 공짜로 처리하고,
**①(붙여넣기 정규화)는 `selection.js:665` 가드를 이벤트 대상 기준으로 넓히는 ~3줄이면 된다.**
"편집 세션 레지스트리"라는 새 개념은 **⑤(캐럿)만 놓고 따로 판단**한다.

---

## 3. 다음에 할 일 (계획 문서 순서)

1. ~~**1단계 나머지** — 검수 칩 rAF 가드를 세대 번호로 교체~~ ✅ **완료(대장 D13)**.
   예상대로 정체성 가드는 한 번도 트립하지 않았고, **계획이 몰랐던 두 번째 결함**이 함께 나왔다 —
   폰트 대기 표식이 문서당 하나라 `fonts.status` 가 'loading' 으로 되돌아간 뒤의 측정이 영영
   재예약되지 않았다(조용한 초록의 실체). 판정은 `scheduleCoverage.run` 한 곳으로 모았다.
2. ~~**2단계** — ① = R1-a(가드 확장)~~ ✅ **완료(대장 D15)**. ⑤ 판단도 끝났다 — **레지스트리 기각**:
   재렌더 후 `session.el` 은 죽은 노드라 복원에 못 쓰고 필요한 건 **재탐색 키**다. 세 모듈이
   `captureEdit()`/`restoreEdit()` 같은 인터페이스를 내는 쪽이 싸다(계획 2단계 주석 참조).
   부수로 **D14 발견**(미수정): `insertHTML` 이 정규화 산출을 다시 오염시킨다.
   수용 기준은 계획 v3 의 (a)~(f) 를 **그대로** 쓸 것(v1·v2 기준은 판별력이 없어 두 번 폐기됐다).
3. **3단계** — 파생 뷰 5경로를 `refreshDerived({level})` 로 수렴 + ⑨(`commitTitle` 을 `applyDocOp` 경유).
4. **4단계** — ⑩ 빈 페이지 드롭 · ⑪ 프리셋 삭제 확인.
5. **5단계** — ④ 다쪽 float 축소(**보류 해제됨**). 착수 전 옆 세션의 `6b7cfd6` 을 먼저 읽고 **재현부터 다시**.
6. **별건** — ② 셀 범위 선택. 제스처 소유권 결정 문서가 선행(계획 문서 참조).

---

## 4. 반드시 알고 시작할 함정 (전부 이번 세션에 실제로 당한 것)

1. **`editorStyle.js` 의 CSS 는 JS 템플릿 리터럴 안이다.** 주석에 백틱을 쓰면 리터럴이 끊겨
   **편집기 전체가 부팅 실패**한다(`".pill is not a function`). 파일에 경고 주석을 남겨 뒀다.
2. **프로브가 "모든 높이 0"으로 나오면 문서 문제가 아니라 부팅 실패다.** `consoleErrors` 는
   `"Uncaught"` 만 주므로 **Playwright MCP 로 같은 URL 을 열어 콘솔 전문을 읽어라.**
3. **`edit-ui` 서버는 ES 모듈을 기동 시 한 번만 로드한다.** `src/usecases/*`·`src/adapters/*` 를
   고쳤으면 **서버 재시작**이 필요하다(정적 자산 CSS/HTML·`src/editor/*` 는 요청마다 읽는다).
4. **유휴 30초 자동저장이 프로브 문서를 오염시킨다**(`saveController` 의 `autosaveMs`).
   프로브가 개체를 지운 채 30초를 넘기면 그대로 저장된다 → **프로브용 문서는 실행 전마다 재생성**.
5. **캔버스 iframe 은 둘(교사·학생)이고 하나는 숨어 있다.** 첫 번째를 집으면 숨은 쪽을 재서 모든
   rect 가 0 이 된다. `scratchpad/probe-lib.mjs` 가 **보이는 프레임**을 고르게 해 뒀다.
6. **합성 이벤트는 이 저장소에서 반복적으로 결함을 통과시켰다.** 배선 검증은 반드시
   `test/helpers/cdp.js` 의 실마우스·실키보드로. `rightClick`·`hover` 도 추가해 뒀다.
7. **변이 실험 없이 초록을 믿지 마라.** 이번에 수용 기준이 **두 번** 무의미했다(v1·v2). 그리고
   ⑥의 렌더 테스트는 **변이를 못 잡았다** — 아래 5항 참조.

---

## 5. 미해결로 넘기는 것 (정직하게)

- **⑥ 회귀 테스트가 성립하지 않는다.** `setMode` 세대 가드를 지워도 렌더 테스트가 초록이다.
  CDP 클릭 간격(160ms)이 학생 프레임 재렌더 창 안에 떨어지지 않아 경합이 재현되지 않는다.
  현재 테스트는 **불변식 스모크**로 표시해 뒀다(회귀 방어 아님).
  → **필요한 것**: 서버 응답을 의도적으로 늦추는 테스트 훅(예: 렌더 라우트가 `?delayMs=` 존중).
    그걸 만든 뒤 회귀 테스트로 승급할 것.
- **실 클립보드 붙여넣기 도구 없음.** 합성 `paste` 는 브라우저 기본 삽입을 일으키지 않아 "고쳤다"를
  증명하지 못한다. 대안은 `execCommand('insertHTML')` 재현 + `defaultPrevented` 단정(계획 v3 참조).
- ~~**대장에 추가해야 할 미보고 2건**~~ ✅ **등재 완료** — 대장 §B-2 의 C16(`runReflow` 가
  `updateAll()`/`refreshLayers()` 미호출 → 레이어 목록이 낡음) · C17(`commitTitle` 이 관문을 우회해
  **D10 과 같은 결함이 제목 커밋 경로에 살아 있다**). 둘 다 계획 3단계에서 닫는다.
- **변이 실험 도구 주의(이번에 당함)**: 이 저장소 소스는 **CRLF** 다. 문자열 `\n` 으로 줄을 지우는
  변이 스크립트는 **조용히 미적용**되고 그대로 초록이 나온다("변이를 못 잡았다"로 오독하기 쉽다).
  줄 단위로 고치고 **적용 줄 수를 스스로 단정**할 것 — `scratchpad/mutate.mjs` 참고.
- **브라우저 모듈 단위 테스트에서 중첩 `data:` URL 은 base64 로**: `encodeURIComponent` 는 작은따옴표를
  인코딩하지 않아 안쪽 URL 을 `from '…'` 에 넣는 순간 문자열이 끊긴다(`review-chip-stale.test.js` 머리말).
- **미선택 float 은 몸통 클릭으로 선택되지 않는다**(래퍼가 `pointer-events:none` — ⠿ 가 유일한 진입점).
  프로브에서 float 선택이 0으로 나오면 결함이 아니라 설계다.

---

## 6. 환경

- 편집기 서버: **:7801** (사용자 문서 `편집기시험0728`, rev 2, 수정 반영됨) · **:7812** (조사용 `조사0729`)
  띄우기: `node bin/worksheet-grab.js edit-ui <문서명> --port <n>`
- 테스트: `npm run test:unit` · `npm run test:render`(직렬 전용 — 두 세션이 동시에 돌리면 Chrome 경합)
- 프로브: `scratchpad/probe*.mjs` (일회용, gitignore). `PROBE_URL` 로 대상 서버 지정.
