# worksheet-grab

한국 K-12 교사용 활동지(활동지) 제작 서비스 — 생성·편집·내보내기. 사용자 구독 AI가 엔진(무API). 설계는 `docs/PLAN.md`(Clean Architecture).

## ⚠ 병행 세션 — 착수 전 30초 확인

이 저장소는 **여러 AI 세션이 한 작업 트리를 동시에 쓰는 일이 실제로 있다**(2026-07-28 관측). 착수 전에:

```bash
git status --porcelain   # 내가 안 건드린 파일이 M 이면 = 다른 세션이 살아 있다
```

비어 있지 않다면 **그 상태에서 잰 테스트 숫자는 기준선이 아니다** — 실제로 남의 미완성 저장 때문에 단위 스위트가 빨간불이었고, 렌더 스위트는 파일을 쓰는 도중 읽어 허위 실패(`normalizeObjectives is not defined`)를 냈다. 내가 고칠 파일이 이미 남의 손에 있으면 **멈추고 사용자에게 알린다.**

절대 금지 3가지(남의 커밋 안 된 작업이 말없이 사라진다): **`git add -A`/`commit -a`**(경로를 명시해 스테이징) · **브랜치 생성·전환**(HEAD 는 트리 전체에 하나라 남의 커밋을 끌어간다) · **`stash`/`reset --hard`/`checkout --`/`clean`**.

전문·근거·worktree 격리 방법은 **`docs/CONCURRENT-SESSIONS.md`**.

## 하네스: 활동지 제작 파이프라인

**목표:** 교사의 한 문장 요청을 학생용/교사용 A4 PDF 2벌로. 활동지 상단 표기는 성취기준 원문이 아니라 해당 차시에 맞게 구체화한 **학습목표**(저작 영역, "~할 수 있다") 중심 — 성취기준 원문은 교사용에만 "근거 성취기준"으로 병기하며 gepai에서만 조회(AI 불변, 원칙 3). 저작권 지문은 교사 직접 입력 + 명시 요청 시 AI 창작·재구성 허용(실존 저작물 원문 재현은 금지, 3층 정책), 범교과(국어 비특화).

**트리거:** 활동지/워크시트/학습지 생성·편집·내보내기 요청 시 `worksheet-grab` 오케스트레이터 스킬을 사용하라. 단순 질문은 직접 응답 가능. 협의/공동설계 신호("같이 설계하자", "딸깍 말고", "먼저 질문해줘") 또는 교과·학년·주제 결손 시에만 `worksheet-consult` 를 조건부 발동한다(완결 요청의 빠른 경로는 불가침).

**팀:** curriculum-mapper → worksheet-planner → worksheet-designer → worksheet-reviewer(검수 게이트) → worksheet-exporter (에이전트 팀, Pipeline + Producer-Reviewer). 상세는 `.claude/agents/`, `.claude/skills/`.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-20 | 초기 구성 (에이전트 5 + 스킬 6) | 전체 | 하네스 신규 구축 |
| 2026-07-22 | k-teacher 하이브리드 이식 — worksheet-consult 스킬 + Gate v2 verbatim 번들 + 조건부 발동(Phase 1/1.5) + 00_brief.json + brief-fidelity advisory | .claude/skills, .claude/agents | 협의형 공동 설계 경로 도입(빠른 경로 보존) |
| 2026-07-23 | editor-v4 US-13(S3.4) — 오케스트레이터 산출물 계약을 개체 트리로 교체: `02_outline.json`(카탈로그 10종·qtype 7종 어휘)→`03_worksheet.json`(개체 트리, `pagination:'scaffold'`)→`04_review.json`(1·2층 findings)→페이지네이션 패스(S3.5/US-14 예정)→export. curriculum-mapper std-box 소비 정합, worksheet-planner 아웃라인 어휘 정합 | .claude/skills/worksheet-grab, .claude/agents/curriculum-mapper.md, .claude/agents/worksheet-planner.md | S3.1~S3.3(designer/reviewer/exporter 개체 트리 계약 개정)에 오케스트레이터·업스트림 계약을 정합, 빠른 경로/consult 발동 로직은 불변 |
| 2026-07-23 | 저작권 지문 슬롯 정책 최종형(3층, 당일 2차 델타로 갱신) — ① 교사 직접 입력: `passage-slot.bodyHtml`/`source`를 편집기에서 직접 입력·수정(저작권법 제25조, 로컬 처리·교사 책임). ② AI 지문 작업(명시 요청 시 허용): `AI_EXCLUDED_TYPES`에서 `passage-slot` 제거(std-box만 잔류) — 교사가 명시 요청하면 AI가 순수 창작 또는 기존 글 재구성/수준 조정/요약 가능, 단 실존 저작물 원문 그대로 재현은 금지(프롬프트 계약). AI 패널에 "창작 지문 생성"·"지문 재구성" 프리셋 신설. ③ std-box(성취기준)는 계속 AI 불변(원칙 3, 무변경). 검수: 저작권을 fail-closed 게이트에서 제외하고 advisory(`passage-copyright`)로 강등 — `source` 미기재·실존 저작물 재현 의심만 권고. `IMMUTABLE_SLOT_TYPES`→`AI_EXCLUDED_TYPES` 개명(개념 명확화, 1차 델타에서 시작) | src/domain/schema, src/usecases/ValidateObjectTree.js·RenderObjectTree.js·aiBridge.js·MigrateManifestToObjectTree.js, src/editor/selection.js·inspector.js·ai.js, .claude/agents/worksheet-designer.md·worksheet-reviewer.md, .claude/skills/worksheet-design·worksheet-review | 사용자 결정 — 1차: "AI 불변, 교사 편집 가능" 2층 분리로 교사 직접 입력 허용. 2차(당일 델타): "AI도 명시 요청 시 지문 생성/재구성 가능"으로 한 단계 더 완화(실존 저작물 원문 재현만 금지) — 실사용성 확대 |
| 2026-07-23 | 학습목표 표기 전환 — 활동지 상단을 "성취기준"에서 "학습목표" 중심으로 개정: `std-box.objectives?:string[]` 신설(codes와 달리 **저작 영역** — 성취기준에서 차시 학습목표를 구체화, 원칙 3 무변경은 "성취기준 원문"에만 적용). objectives 있으면 학생/교사 공통 "학습 목표" 박스 + 교사 전용 "근거 성취기준"(코드+원문, `.std-ref` data-mode CSS로만 숨김 — 정답 아니라 물리 제거 불필요)을 렌더, 없으면(하위호환) 현행 성취기준 박스 그대로. 레거시 결정적 엔진(`AssembleWorksheet#renderStandardLabel`)은 학습목표 저작이 불가하므로 기계 변환으로 표기만 개선(코드 뗀 성취기준 문장 = 목표, 교사용에 코드+원문 병기). 편집기 인스펙터에 objectives 편집 필드(줄 단위) 추가, codes는 읽기 전용으로 전환. planner가 objectives를 저작해 outline에 담고 designer가 std-box로 옮기며, reviewer는 학습목표-성취기준 대응을 advisory로 확인 | src/domain/schema/ObjectCatalog.js, schema/worksheet-object.schema.json, src/usecases/RenderObjectTree.js·AssembleWorksheet.js, assets/blocks.css, src/editor/inspector.js·editor.js, .claude/agents/worksheet-planner.md·worksheet-designer.md·worksheet-reviewer.md, .claude/skills/worksheet-plan·worksheet-design·worksheet-review | 현장 관행(교사 지적) — 활동지에는 성취기준 원문이 아니라 차시에 맞게 구체화한 학습목표를 제시, 성취기준 원문은 교사의 평가 근거 참조용으로만 병기 |
| 2026-07-27 | editor-v4 Phase 5 — 모듈 경계 정리(동작 무변경 리팩터링). ① 편집기 controller 분리: `editor.js` 1,241→884줄, 저장(`saveController`)·내보내기/미리보기(`exportController`)·검수 칩(`reviewChip`)·배너(`banner`)·단축키/클립보드(`shortcuts`)·편집 CSS(`editorStyle`) 를 `create*(deps)` 팩토리로 분리. `applyDocOp` 은 `editor.js` 가 계속 소유(문서 변경 단일 관문 — 분리 모듈은 next 문서만 계산해 주입받은 관문으로 보낸다). ② 서버 route 분리: `EditorHttpServer.js` 550→84줄(조립 지점), 라우트 15개를 `adapters/editor-routes/` 6모듈 + 선언적 테이블(`httpKit.dispatch`)로 교체 — 405/404 폴백 순서와 PASS 흘려보내기로 구 `if` 사슬 동작 재현. 보안 성질(화이트리스트·경로 이탈·매직바이트·렌더 in-flight 가드)은 각 1모듈에 집중. ③ 진짜 중복만 통합: `deriveRenderMeta`(RenderObjectTree 소유 — 편집기 reflow 도 같은 함수 사용, R2-1 동치 근거)·`loadRenderAssets`·`loadKnownSubjectHexes`·`buildCanvasMeta`. **`SaveDocument.execute`(HTML manifest)와 `checkpoint`(개체 트리)는 입력 스키마가 달라 합치지 않음** — 레거시 지연 마이그레이션 유지 | src/editor/{editor,saveController,exportController,reviewChip,banner,shortcuts,editorStyle,reflow}.js, src/adapters/EditorHttpServer.js·editor-routes/*, src/usecases/{RenderObjectTree,RenderEditorShell,SaveDocument,RunPipeline,paper,renderAssets}.js, src/cli/index.js, docs/HANDOFF-editor-workspace.md | PRD §9 Phase 5 — 기능 추가 없이 경계만 정리. 단위 467/467(기준선 463 + 계약 4건 신규)·렌더 91/91·fail 0 — 테스트 개수 감소 0·기대값 수정 0(기존 테스트 파일 무변경). 리뷰 지적 반영: `readBinaryBody` 가 분리 과정에서 `cap` 기본값을 잃어 상한이 사라질 수 있던 fail-open 을 fail-closed 로 되돌리고 테스트로 고정. 동작 무변경 근거: 구 `if` 사슬과 라우트 매칭 케이스별 대조, `injectEditorStyle` CSS 가 원본과 바이트 동일(LF 정규화 5,871자), 실 Chrome CDP 실입력을 **HEAD 워크트리와 A/B 실행해 항목별 결과 완전 동일** |
| 2026-07-28 | 배치 UX 3건 + 레이아웃 타입 2종 — ① **본체 드래그 재정렬**: flow 개체를 ⠿ 손잡이뿐 아니라 몸통 아무 데나 잡고 끌 수 있다(5px 임계 승격, 편집·마퀴·표 손잡이·float 과 배타). ② **flow→float 실측 승격**: `toggleFlowFloat(doc,id,measuredRect)` — 개체가 보이던 자리에 그대로 고정(종전 `{20,20,60,30}` 하드코딩으로 좌상단에 튀던 것). 강등 시 rect 삭제 계약을 순수 함수 안에 둠. ③ **자유 배치 advisory 3종**(`src/editor/floatLayout.js` 신설): 겹침·지면 이탈(순수) + 본문 덮기(DOM 측정), 전건 `warning` 이며 export 게이트 무접촉. 회전(`angle!==0`)·`shape` 은 겹침 규칙에서 제외(실문서 실측 근거 — 도형이 본문을 덮는 것은 정상 저작). ④ **닫힌 카탈로그 10 → 12종**: `spacer`(빈 공간, `heightMm`)·`page-break`(페이지 나누기) 신설 — 둘 다 flow 전용·**편집기 전용**(AI 저작 어휘 아님). 승격이 비운 자리를 되찾는 수단과 "여기서 끊어라"를 표현할 어휘가 없었다. `page-break` 는 페이지 **용량을 늘리지 않는다**(끊는 위치만 정함) — 개행은 CSS 가 아니라 `assignFlowToPages` 의 `breakBefore` 로만 일으켜 D-A(페이지 경계 단일 권한)를 지킨다 | src/editor/{canvasInline,selection,editor,editorStyle,objectFactory,reviewChip,inspector,floatLayout}.js, src/usecases/{RenderObjectTree,PaginateObjectTree}.js, src/domain/schema/ObjectCatalog.js, schema/worksheet-object.schema.json, assets/paper.css, .claude/agents·skills, docs/HANDOFF-object-schema.md | 사용자 요청 — "전면 float(Canva식) 전환" 은 리플로우 소멸·학생용 2벌 구멍·원칙 3 위반으로 기각하고 flow 모델 유지 + 조작감만 개선. 실사용 테스트에서 나온 후속 요구(빈 공간·페이지 고정)를 레이아웃 타입 2종으로 흡수 |
| 2026-07-28 | 편집기 UX 5건(사용자 피드백) — ① **근거 성취기준 기본 미표시**: `std-box.showStandards?:boolean` 신설(기본 false). 종전엔 objectives 가 있으면 "근거 성취기준"이 항상 따라 나왔으나, 현장에서 활동지에 얹는 것은 학습목표뿐이라는 피드백에 따라 **명시적으로 켤 때만** 렌더한다(켰을 때 동작은 종전과 동일 — `.std-ref` 교사 전용). 표시 여부만 정하고 `codes` 는 보존. designer 는 기본적으로 싣지 않는다. ② **본문 인라인 편집 확대**: `.q-part`(문항 선지) 규약을 `.wg-part[data-part]` 로 넓혀 학습목표 문장·박스 제목(`std-box.heading` 신설)·제목 배지/모서리/출처(`meta.*`)·지문 제목/출처·표 캡션을 캔버스에서 더블클릭해 고친다(종전 인스펙터 전용). `partEdit.js` 를 문항 전용 → 타입/필드 허용목록(`EDITABLE_PARTS`) 기반으로 일반화, Enter=편집 종료(조각 값 개행 오염 차단). 좌표는 **editMode 속성으로만** 실려 인쇄 산출·레이아웃 박스 불변(R2-1). ③ **이미지 자리 UI**: `.image-slot` CSS 가 blocks.css 에 전무해 빈 자리가 맨 글자로만 보이던 것(실 Chrome 관측)을 아이콘+점선 박스로 교체, 채운 이미지에 `max-width:100%` 부여. 편집기 안내 문구는 렌더에 넣지 않는다(학생 배포본 공유 자산). ④ **지문 박스 서식**: `passage-slot.borderColor/borderWidth/bgColor` 신설 — table 과 동형 CSS 변수 경로, 값은 `cssColor()` 통과분만 방출(인라인 style 선언 주입 차단, fail-closed). ⑤ **연결형 연결점 간격**: 가운데 칸 한복판에 붙어 있던 `·&nbsp;&nbsp;&nbsp;·` 를 좌우 끝으로 벌려 각 항목 옆에 붙임(flex space-between, td 는 table-cell 유지). 그 밖: 문항 항목 개수 증감(객관식 보기·연결 쌍·순서 항목 — 종전엔 삽입 기본값에 갇혀 5번째 보기를 못 만들었다)·서술형 답란 줄 수 인스펙터 노출, 답란 스타일 라벨 한글화(line/dots/box → 밑줄/점/네모 칸), 지문 인스펙터 안내문을 3층 정책 현재 내용으로 정정(종전 문구는 "AI는 지문을 재작성하지 않는다"고 단정 — 2차 델타 이후 사실과 어긋났다), 제목 출처(`.title-src`) 스타일 신설(무스타일 맨 글자였다) | src/usecases/RenderObjectTree.js, src/domain/schema/ObjectCatalog.js, schema/worksheet-object.schema.json, assets/blocks.css, src/editor/{partEdit,inspector,editorStyle,testSeed}.js, docs/HANDOFF-object-schema.md §12, .claude/agents/worksheet-designer.md, .claude/skills/worksheet-design | 사용자 피드백 — 실 Chrome 렌더로 4건 전부 현물 확인 후 착수(성취기준 강제 병기·이미지 자리 맨 글자·지문 서식 부재·연결점 뭉침). 검증: 단위 신규 3파일 포함 전량 통과 + 실 Chrome 시드(`part-edit-ux`)로 인라인 편집·표시 토글·테두리 계산색·점 간격 실측·항목 증감·플레이스홀더 높이 단정 |
| 2026-07-28 | 레거시 결정적 엔진(`AssembleWorksheet`, `gen:'standard-label'`)도 **학습목표 저작 가능**으로 전환 — 종전엔 성취기준 원문만 조립해 목표를 지을 수 없었고(원칙 3 때문이 아니라 엔진 한계) 코드 뗀 성취기준 문장을 기계 변환해 목표 자리에 썼다. `manifest.objectives`(저작 문장 배열)·`objectivesHeading`(박스 제목)·`showStandards`(근거 성취기준 표기, 기본 false) 3필드를 열어 개체 트리 `std-box.objectives/heading/showStandards` 와 **같은 계약**으로 맞췄다. 원문 조회 규칙은 불변(`#resolveStandards` 는 여전히 창작 없이 CSV/MCP/폴백만, 못 찾으면 throw — 목표 저작이 원문 창작 금지를 완화하지 않는다). 미저작 시 산출은 **바이트 동일**(하위호환 — 저작 문장이 없는 문서에서 근거까지 숨기면 성취기준 정보가 화면에서 사라지므로 종전 2박스 유지). `MigrateManifestToObjectTree` 가 세 필드를 `std-box` 로 승계(없으면 레거시 문서를 편집기에서 여는 순간 교사가 쓴 목표가 소실). `Worksheet.objectives` 는 `Standard[]` 가 아닌 문자열 배열(조회 원문이 아니라 저작 문장 — 원문 주입 강제 대상 아님). 저작 통로: `ComposeWorksheet`/`GenerateWorksheet` 인자 + CLI `--objectives "문장1\|문장2"`·`--objectives-heading`·`--show-standards`(구분자가 `\|` 인 이유 — 목표 문장에 쉼표가 흔하다) | src/usecases/{AssembleWorksheet,GenerateWorksheet,ComposeWorksheet,ArchetypeLibrary,MigrateManifestToObjectTree}.js, src/domain/Worksheet.js, src/cli/index.js, docs/HANDOFF-object-schema.md §12-5 | 사용자 요청 — 개체 트리 경로만 학습목표를 저작할 수 있고 레거시 경로는 못 하던 비대칭 해소. 검증: 신규 단위 15건(하위호환 바이트 동일·이스케이프·원문 창작 금지 유지·마이그레이션 승계 포함) + 실 CLI `generate --objectives --show-standards` 로 교사용 PNG 실관측(저작 목표 2줄 + 근거 성취기준 3코드) |
| 2026-07-28 | 개체 크기 조정(flow) — 교사가 표·제목 등 **본문 배치 개체의 가로·세로를 직접 조절**한다(종전엔 자유 배치만 가능했고 flow 는 수단 전무, 표는 열 너비만). **크기 필드 3종 신설**: `widthPct`(본문 폭=`.sheet-body` 열 폭 대비 %, 5~100)·`minHeightMm`(최소 높이)·`align`(좌우 정렬) — flow 전용이며 float 은 `size-forbidden-in-float`(rect 가 이미 크기를 갖는다). ① **mm 가 아니라 %인 이유**: 포함 블록이 열 폭이라 용지·단 수를 바꾸면 mm 는 열을 넘겨 클램프가 필요하고, 클램프는 값을 되돌릴 수 없게 덮어쓴다(%는 어느 폭에서도 유효). 교사 직관은 인스펙터의 mm 환산 표시로 흡수. ② **height 가 아니라 minHeight**: 고정 높이는 내용이 넘칠 때 잘리는데 그 넘침이 측정에 안 잡혀 페이지 경계가 조용히 어긋난다. ③ **크기는 좌표가 아니다** — 원칙 3은 "AI 가 위치를 지어내는 것"을 막을 뿐이라 `rect-forbidden-in-flow` 는 무변경. 단 **AI 저작 어휘에서는 제외**(spacer/page-break 와 동급 편집기 전용). ④ **R2-1 급소**: `.wg-obj` flow 래퍼가 원래 `editMode` 에서만 방출돼, 핸드오프대로 래퍼에 style 을 얹었다면 폭이 인쇄에서 빠져 편집==인쇄가 깨졌다 → 방출 조건을 **`editMode \|\| 선언있음`** 으로 전환(선언 없는 개체의 인쇄 출력은 바이트 동일 = 회귀 0). ⑤ **손잡이는 오버레이 3방향**(e·s·se) — `.wg-obj` 안에 넣으면 richtext 의 readField(innerHTML)가 손잡이를 본문으로 저장한다(canvasInline 규약). ⑥ 클램프는 `resizeFlow`(순수) 단일 관문 — 드래그·인스펙터 공유. **변이 실험으로 확인된 하네스 한계**: 기존 파리티 3자 대조는 두 측정 경로가 모두 `editMode:true` 라 "편집에만 반영되는 결함"을 못 잡는다 → 인쇄 HTML 직접 단정이 실제 검출기 | src/domain/schema/{ObjectCatalog,validateObjectShape,index}.js, schema/worksheet-object.schema.json, src/usecases/RenderObjectTree.js, src/editor/{objectFactory,canvasInline,editorStyle,inspector,editor}.js, test/helpers/cdp.js, .claude/agents/worksheet-designer.md, .claude/skills/worksheet-design, docs/DECISION-object-resize.md | 사용자 요청 — "표 크기를 못 바꾼다". 격리 worktree(`feat/object-resize`)에서 진행: 단위 670→694·렌더 111→118·fail 0·skip 0. 실마우스(CDP) 검증이 배선 결함 1건 적발(선택 변경 시 오버레이 미갱신 — 합성 이벤트로는 잡히지 않음) |
| 2026-07-28 | 테스트 임시 디렉터리 누수 정리 — **실행 1회당 잔존 224건 → 0건**. 감사 결과 단위는 24개 파일 전부가 `mkdtemp` 54곳에 `rmSync` 0곳(153건/회), 렌더는 31개 중 21개가 누수(71건/회)였고 렌더는 지점이 파일당 둘이었다(Chrome 프로필=정리하나 EPERM 이면 새고, 워크스페이스 base=아예 무정리). `test/helpers/tmp.js` 에 **자동 정리판 `autoTmpDir()`/`autoTmpDirSync()`** 신설 — 기존 `makeTmpDir()`(호출부가 finally 에서 `cleanup()`)는 서버 종료 순서를 챙기는 렌더엔 맞지만, `finally` 를 가진 단위 파일이 24개 중 5개뿐이라 그 관례를 강요하면 54곳을 구조 개편해야 했다. 이제 정리 통로가 3겹(즉시 → 파일 종료 `after()` → 60분 스윕)이고, `makeTmpDir*` 산출물도 추적 목록에 실어 **즉시 정리 실패분을 종료 훅이 재시도**한다. 실측으로 드러난 함정 2건 — ① **`after()` 는 모듈 최상위 등록이 필수**: "첫 사용 때 등록"하는 지연 패턴은 그 첫 호출이 이미 테스트 **안**이라 훅이 파일이 아니라 그 테스트에 붙어 **3개 중 1개만** 지운다. ② **잔존 71건은 영구 잠김이 아니라 핸들 지연**: 몇 분 뒤 같은 목록을 지우니 79건 중 78건이 삭제돼, `rmSync` 기본 재시도(≈300ms)가 Chrome·서버 종료 직후 핸들 해제보다 짧았음이 확인됨 → 훅을 2패스(실패분만 800ms 후 재시도)로 바꾸자 렌더 잔존 71→0. **헬퍼 자체 회귀 테스트 3건 신설**(자식 TMPDIR 샌드박스 — 단위 스위트는 파일 간 병렬이라 진짜 `os.tmpdir()` 를 쓸면 동시 실행 중인 남의 tmp 를 지운다). 그 테스트는 처음에 헛통과했다 — 부모가 `node --test` 라 `NODE_TEST_CONTEXT=child-v8` 이 손자에 상속돼 손자 러너가 **0건 실행·종료코드 0** 으로 끝났고(만든 게 없으니 잔존 0), 변이 실험으로 적발해 환경변수 차단 + 실행 건수 단정을 넣었다. 곁들여 `DECISION-object-resize.md` Phase 0 의 `sweepStaleWsgTmp(0)` 처방을 정정(나이 인자는 성능 옵션이 아니라 **병행 세션 안전장치** — `(0)` 은 돌고 있는 남의 Chrome dir·출력 PDF 까지 지워 "바이트는 썼는데 파일이 없다"는 렌더 실패로 나타난다) | test/helpers/tmp.js, test/unit/tmp-helper.test.js(신규), test/unit/*.test.js 24개, test/render/*.render.test.js 21개, docs/DECISION-object-resize.md | 병행 세션 중단분 인수 — 다른 세션이 감사만 해 두고 멈춘 "렌더 9개가 손수 rmSync" 를 이어받아 실측하니 범위가 45개 파일이었다. 검증: 단위 718/718(기준선 715 + 신규 3)·렌더 118/118·fail 0·skip 0, 양쪽 실행 후 잔존 0, 변이 5종 전부 신규 테스트가 적발. `editor-ai` 플레이크(다른 세션 관측)는 단독 5회 + 전량 2회 = 7연속 무결로 미재현 |
| 2026-07-28 | 개체 조작 손잡이 — 실마우스 결함 수정 + **상시 노출 제거**. ① **⠿ 를 실제로 잡을 수 있게**(종전엔 합성 이벤트 테스트만 통과하고 실마우스로는 안 잡혔다): 원인이 둘이었다 — (A) `attach()` 의 최초 장식이 레이아웃 확정 전에 돌아 손잡이가 전부 한 자리에 쌓였고(실측: 개체 82/116/132/166 인데 손잡이 4개 전부 top=23), 선택을 바꿔 재장식해야만 제자리를 찾았다 → `scheduleHandleReflow`(rAF 2회 + `fonts.ready` + sheet ResizeObserver, **드래그 중 무동작** — 오버레이 교체는 포인터 캡처를 끊는다). (B) 좌표가 맞아도 `elementFromPoint(⠿ 중심)` 가 늘 `.wg-flow-insert` 였다 — 둘 다 `left:-22px` 인데 컨트롤 18px 대 본문 개체 높이 20px 이라 세로로 갈라 놓을 공간이 없었다(divider 2px 는 완전 겹침) → `+` 를 `left:-44px` 로 **가로 분리**(세로 위치는 "뒤에 삽입" 의미라 유지) + ⠿ 에 z-index 우선. ② **조작 칩 hover 전용화**(flow ⠿·+ 와 float ⠿ 전부): 종전엔 개체마다 칩이 항상 떠 있어 여백이 칩으로 덮였다 — 사용자 불만의 실체는 기능이 아니라 상시 노출이었다. 기본 `opacity:0`, 가리킨 개체의 것만 `.is-hot` 으로 표시하고, **칩 자신 위도 같은 개체로 친다**(오버레이가 `.wg-obj` 밖이라 다가가는 도중 꺼지면 못 잡는다). 드래그 중에는 추적 정지, `pointer-events` 는 숨은 동안에도 auto(히트테스트 동작 불변), `+` 는 `:focus-visible` 로도 노출(키보드). ③ **없애자는 앞선 논의는 기각**: 그 근거("두 손잡이 다 어차피 안 잡히는 장식")가 ①로 사라졌고, 실측상 ⠿ 는 얇은 개체의 유일한 진입점이다 — 편집 모드 몸통 높이가 page-break **0px**·divider 2px 라 본체 드래그가 성립하지 않는다. editMode 래퍼에 높이를 주는 우회는 편집 레이아웃만 키워 R2-1 을 깬다. float ⠿ 도 미선택 래퍼 `pointer-events:none` 의 유일한 탈출구라 존치. 그 밖: `test/helpers/cdp.js` 에 `hover(x,y)` 추가(hover 로만 드러나는 UI 는 `click` 으로 검증 불가), 어제 추가한 스윕 테스트의 자기 플레이크 수정(최선노력 삭제의 1회 결과를 단정 → CPU 부하에서 5회 중 2회 실패, 재시도로 바꿔 부하 4개 아래 12/12) | src/editor/{canvasInline,editorStyle,selection}.js, test/render/editor-flow-handle.render.test.js(신규), test/helpers/cdp.js, test/unit/tmp-helper.test.js, scratchpad/probe-flow-body-drag.mjs | 사용자 피드백 — "⠿ 가 거슬린다". 실 Chrome CDP 실마우스로 재현·검증: 최초 장식부터 손잡이가 제자리, 히트테스트 `.wg-flow-handle`, 드래그로 a1,a2,a3,a4→a3,a1,a2,a4, 칩 8개 미가리킴 시 전부 opacity 0·가리킨 것만 0.55, float 은 미선택 상태로 100mm→113.2mm 이동(크기 불변). 렌더 118→**123**·단위 715→**718**·fail 0·skip 0, 변이 총 8종 전부 테스트가 적발 |
| 2026-07-28 | 편집기 결함 5건 — **Codex 교차 점검 + 실마우스 검증**. 두 경로가 잡는 결함이 갈렸다: 코드 정독으로만 보이는 것(저장 경합)과 실입력으로만 드러나는 것(히트테스트·포인터 캡처)이 **한 건도 겹치지 않았다**. ① **[P0] 편집 중 Backspace 가 개체를 통째로 삭제**(실측: 표 4→3, `std-box` 1→0) — `isTypingContext` 가 공통 `editingId` 와 **부모 문서** `activeElement` 만 봤는데, 표 셀(tableEdit)·조각(partEdit)은 지역 상태만 세우고 캐럿이 iframe 안이면 부모 activeElement 는 `<iframe>` 요소라 contentEditable 이 아니다 — 두 판정이 다 false 라 삭제 단축키가 그대로 통과했다 → **이벤트 대상(`e.target`)을 먼저 본다**(핸들러가 부모·teacher iframe 양쪽에 걸려 있으므로 그것으로 족하다). ② **[P0] 저장 왕복 중 편집 유실** — 응답의 `result.document` 를 무조건 채택하고 `dirty` 를 내려, 수 초짜리 왕복(서버가 재렌더한다) 동안 친 글자가 **응답 도착 순간 사라지고** 자동저장도 되살리지 못했다 → `editSeq`(markDirty 마다 증가)를 요청 시점과 대조해 그 사이 편집이 있으면 서버 문서를 채택하지 않고 dirty 를 유지한다(정규화는 다음 저장에서 다시 받는다 — 멱등). 배너도 "저장하는 동안 수정한 내용은 아직 저장 전"이라고 밝힌다. ③ **[P1] 크기 손잡이 드래그가 마퀴를 동시 발동**해 드롭에서 선택이 사라졌다(연속 조정 불가) — 마퀴 판정이 오버레이(`.wg-obj` 밖) 손잡이를 빈 배경으로 보고 시작했고 `finishMarquee` 가 선택을 float 교차분(문서에 float 이 없으면 빈 집합)으로 덮었다. `canvasInline` 의 `stopPropagation()` 은 **같은 `doc` 노드의 형제 리스너를 막지 못한다** → pointerdown 이 `.wg-flow-overlay` 안이면 양보. 오버레이는 시트 전체를 덮지만 `pointer-events:none` 이라 자식(칩) 위에서만 대상이 되므로 마퀴는 무영향(대조군 테스트로 고정). ④ **[P1] 드래그 뒤 다음 클릭 1회가 먹힘**(두 번 눌러야 선택) — 드롭에서 arm 한 `swallowNextClick` 을 소비할 click 이 **아예 오지 않는다**(재장식으로 pointerdown 대상이 DOM 에서 떨어져 브라우저가 click 을 만들지 않는다 — 이벤트 로그 실측 click 0건) → 새 pointerdown 마다 무장 해제(click 은 언제나 다음 pointerdown 보다 먼저 오므로 정상 경로 무손상). ⑤ **[P2] 불가능한 '자유 배치로 전환'이 활성 버튼으로 노출**되고 눌러도 무동작 → `PLACEMENT_TOGGLEABLE_TYPES`(두 배치 다 지원하는 타입) 파생 상수 신설, 인스펙터·컨텍스트 툴바가 같은 계약을 본다 | src/editor/{shortcuts,selection,saveController,inspector,contextToolbar}.js, src/domain/schema/{ObjectCatalog,index}.js, test/render/editor-edit-guard.render.test.js(신규 6건), test/unit/save-controller.test.js(신규 3건) | 사용자 요청 — "편집기에 버그가 꽤 있다, codex 와 교차 점검해 꼼꼼히 개선". 격리 워크트리 `feat/editor-ux-qa` 에서 진행(병행 세션 규약 §1 — 다른 세션이 main 에서 페이지네이션 작업 중이었다). 검증: 단위 719→**722**·렌더 128→**134**·fail 0, **변이 실험 4종 전부 적발**(①③④⑤ 는 수정을 하나씩 되돌리면 해당 테스트가 빨간불, ② 는 수정 **전에** 테스트를 세워 실패를 본 뒤 초록으로 만들었다). 미수정분 10여 건(병합셀 colspan 붕괴·프리셋에 손잡이 DOM 혼입·타이핑+명령 undo 병합 등)은 재현 절차와 함께 `docs/DEFECTS-editor-2026-07-28.md` 에 대장으로 남김 |
| 2026-07-28 | 편집기 결함 2건(사용자 실사용 보고) — ⑥ **조각을 더블클릭하면 글자가 사라진다**: 편집 표식 `.wg-part-editing` 이 배경만 반투명 노랑으로 덮고 글자색은 두는데, 제목 배지 `.pill` 은 청록 바탕에 **흰 글자**라 배경이 사라지는 순간 흰 종이 위 흰 글자가 됐다(다른 조각은 회색 글자·연한 배경이라 멀쩡해서 오래 안 보였다) → 표식에서 `background` 를 빼고 `box-shadow` 바깥 글로만 강조(어떤 배색이 와도 안전, 레이아웃 박스 불변이라 R2-1 무해). ⑦ **세로로 늘려도 실제 테두리 높낮이가 안 바뀐다**: 최소높이를 `.wg-obj` 래퍼에만 얹었는데 블록 자식은 부모 min-height 를 따라 늘지 않고 `.wg-obj` 엔 CSS 가 아예 없어(무스타일 래퍼) 늘어난 만큼이 그냥 빈 여백이 됐다(실측 래퍼 93→112px, `.title-box` 93px 고정) → 최소높이를 준 개체에만 `data-minh` 표식을 방출하고 `paper.css` 가 그 개체의 속 내용을 flex 로 늘린다. 제목은 `.title-wrap` 이 한 겹 더 있어 안쪽 `.title-box` 까지 따로 늘린다(배지 `.pill` 은 `align-items:flex-start` 로 가로 늘어남 방지). **폭만 준 개체엔 붙이지 않는다** — 블록 자식은 이미 폭을 채우고, flex 로 바꾸면 마진 상쇄가 사라져 선언 없던 문서의 조판까지 흔든다. 표식·style 이 인쇄에도 함께 나가 R2-1 유지(단위 4건으로 고정). **자책 기록**: ⑥ 수정 중 주석에 백틱을 써서 편집기를 부팅 불가로 만들었다 — `editorStyle.js` 의 CSS 는 JS 템플릿 리터럴 안이라 주석의 백틱도 리터럴을 끊는다(`".pill is not a function`). 프로브가 "모든 높이 0"으로 나왔을 때 문서 오염을 먼저 의심했으나 실제 원인은 방금 넣은 백틱이었고, 파일에 경고 주석을 남겼다 | src/editor/editorStyle.js, src/usecases/RenderObjectTree.js, assets/paper.css, test/render/editor-edit-guard.render.test.js(+2건), test/unit/render-object-size.test.js(+4건) | 사용자 실사용 보고 2건. 검증: 단위 722→**726**·렌더 134→**136**·fail 0, 변이 3종 전부 적발(그중 하나의 실패 메시지가 사용자 증상 그대로 — `.title-box 66→66, 래퍼 97→187`). 크기 지원 타입 4종(제목·학습목표 박스·표·자유 텍스트) 전부에서 보이는 테두리 상자가 실제로 늘어남을 실측 |
| 2026-07-28 | 편집기 결함 2건(Codex 지적 이어받기) — ⑧ **'내 블록으로 저장'이 편집 손잡이를 인쇄물에 굳힌다**: 저장된 프리셋 html 이 `<div class="wg-float-handle">⠿</div>` 로 시작하고 리사이즈 손잡이 8개까지 담겼다(실측). 그 프리셋은 삽입 시 `richtext.html` 이 되므로 **학생 배포본에 ⠿ 와 파란 사각형이 인쇄된다.** 원인은 제거 규칙이 `selection.js` 의 `readField`/`readHtmlField` **안에만** 있고 `saveObjectAsPreset` 은 날 `innerHTML` 을 보낸 것 — 같은 규칙이 두 벌로 갈라져 있었다 → `innerHtmlWithoutChrome()` 를 export 해 **DOM 을 콘텐츠로 굳히는 모든 경로가 하나를 쓰게** 했다. 곁들여 프리셋 저장이 `res.ok` 를 안 봐서 서버가 거절해도 "저장했습니다"가 뜨던 것도 고쳤다(`fetch` 는 4xx/5xx 에 throw 하지 않는다). ⑨ **병합된 칸을 다시 병합하면 표 구조가 무너진다**: 3열에서 B+C 병합 뒤 A 를 오른쪽 병합하면 그 행이 덮는 열 수가 2(표는 3열)가 되고 숨은 칸이 `colspan:2` 를 그대로 들고 있다. 원인은 `cell.colspan = span + 1` — 흡수 대상이 이미 여러 칸을 덮어도 **항상 +1**만 했다 → 계산을 순수 `mergeCells(rows,{r,c},dir)` 로 떼어 내고 ⓐ 대상 span 합산 ⓑ 숨은 칸 span 제거 ⓒ **축이 어긋나는 병합은 거절**(fail-closed — 조용히 깨진 표보다 무동작이 낫다). 그 밖: `test/helpers/cdp.js` 에 `rightClick(x,y)` 추가(우클릭 메뉴는 좌클릭 헬퍼로 검증 불가, `dispatchEvent('contextmenu')` 는 히트테스트를 건너뛴다) | src/editor/{selection,editor,tableEdit}.js, test/helpers/cdp.js, test/unit/table-merge.test.js(신규 8건), test/render/editor-edit-guard.render.test.js(+1건) | 사용자 요청 "남은 결함 계속 봐". 검증: 단위 726→**734**·렌더 136→**137**·fail 0. ⑨ 는 순수 함수를 먼저 추출해 **빨간불 5건**을 본 뒤 고쳐 초록으로(red→green), ⑧ 은 변이(제거 관문을 날 innerHTML 로 되돌림)로 적발 확인 |
| 2026-07-28 | 편집기 결함 1건 — ⑩ **타이핑 직후의 명령이 직전에 친 글자까지 함께 되돌린다**: 글자를 친 뒤 500ms 안에 복제·삭제·속성 변경을 하고 Ctrl+Z 를 누르면 명령뿐 아니라 방금 친 글자도 사라졌다. 타이핑은 유휴 500ms 로 묶여 확정되는데 `applyDocOp` 이 대기 중인 타이핑을 먼저 확정하지 않고 문서를 바꾼 뒤 `history.commit()` 을 불러 **타이핑과 명령을 한 상태로** 찍었다. 원래 이 목적의 `history.run(fn)` 래퍼가 있었지만 **호출부가 하나도 없는 죽은 코드**였다 → 그 래퍼를 `flushTyping()` 원시 연산으로 교체하고 문서 변경의 단일 관문인 `applyDocOp` 이 문서를 바꾸기 **전에** 부른다. 대기 중 타이핑이 없으면 무동작이고 상태가 같으면 `commit` 이 제자리 갱신하므로 빈 단계가 쌓이지 않는다. **손대지 않기로 한 것**: C5(flow 없는 다쪽 float 문서가 1쪽으로 축소)는 무대가 `reflow.js`·`PaginateObjectTree.js` 인데 다른 세션이 같은 파일을 작업 중이라(`777b728`) 나란히 고치지 않았다(병행 세션 규약 §2-5 — 빨간불의 책임 소재를 잃는다) | src/editor/{history,editor}.js, test/unit/history-flush-typing.test.js(신규 4건) | 사용자 요청 "남은 결함 계속 봐". 검증: 단위 734→**738**·렌더 137·fail 0, 변이(flushTyping 무동작화)로 2건 적발. 연속 타이핑 코얼레싱은 회귀 테스트로 함께 고정 |
| 2026-07-29 | 편집기 잔여 결함 — **계획 0단계·1단계 완결**(계획 `docs/PLAN-editor-remaining-2026-07-29.md`). ⑪ **저장 실패를 무시하고 진행**하던 4경로(용지·테마·PNG·PDF)를 이미 옳은 형태였던 `editor.js:397`(`if (isDirty() && !(await save())) return;`)로 통일 — 저장이 실패해도 화면·산출이 진행돼 교사가 저장된 것으로 믿었다(대장 D11, `f1a902b`). ⑫ **모드 토글 경합** — `setMode` 가 버튼은 동기로, 프레임은 `await` 뒤에 바꾸는데 학생 프레임 재렌더(저장+서버 조립)가 수 초라 그 사이 시작된 두 번째 전환이 먼저 끝나면 **늦게 끝난 첫 호출**이 낡은 클로저 `m` 으로 화면을 되돌렸다 → 세대 번호 `modeSeq`(대장 D12, `1079e20`). **단 회귀 테스트가 성립하지 않아 "불변식 스모크"로 강등** — 가드를 지워도 렌더가 초록이다(CDP 클릭 간격 160ms 가 재렌더 창에 안 떨어진다). 승급하려면 서버 응답을 늦추는 테스트 훅이 필요하다. ⑬ **검수 칩의 낡은 측정이 최신 결과를 덮어쓴다**(대장 D13) — 결함 실체가 둘이었다: (a) 스테일 가드가 Document **정체성**(`getTeacherDoc() !== teacherDoc`)을 보는데 `reloadTeacherFrame` 이 흰 깜빡임을 없애려 iframe 대신 `body.innerHTML` 만 갈아서 정체성이 편집기 수명 내내 유지된다 — 그 가드는 **한 번도 트립하지 않았다**. (b) 폰트 대기 표식이 **문서당 하나**(`fontsWaitingOn === teacherDoc`)라 같은 Document 에서 최초 1회만 대기가 걸린다 → 새 글리프로 `fonts.status` 가 'loading' 으로 되돌아가면 그 뒤 측정은 게이트에 걸린 채 **영영 재예약되지 않아** 검수 결과가 조용히 낡은 채 굳는다. 수정: 검수 세대 `reviewSeq`(runReview 마다 증가)로 교체해 예약된 측정이 **자기 세대가 최신일 때만** 칠하게 하고, 폰트 대기 표식을 세대당 하나로. 폰트 쪽 중복 판정은 제거해 판정을 `scheduleCoverage` 의 `run` **한 곳**에 모았다(같은 판정이 두 벌이면 어느 쪽이 막는지 증명할 수 없다). **대가 명시**: 세대를 "재렌더"가 아니라 "검수 호출"에 붙여 놓치는 재렌더는 없지만(재렌더 3경로가 전부 직후 `runReview` 를 부른다) DOM 이 안 바뀐 검수(저장 완료·제목 커밋)가 비행 중 측정을 취소할 수 있다 — 그때는 모듈 계약대로 직전 측정값을 유지한다(빈 값으로 덮지 않는다). 그 밖: 대장에 미보고 2건 등재(C16 `runReflow` 가 `updateAll`/`refreshLayers` 미호출로 레이어 목록이 낡음 · C17 `commitTitle` 이 관문을 우회해 **D10 과 같은 결함이 제목 커밋 경로에 살아 있다**) | src/editor/reviewChip.js, test/unit/review-chip-stale.test.js(신규 3건), docs/DEFECTS-editor-2026-07-28.md, docs/PLAN-editor-remaining-2026-07-29.md, docs/HANDOFF-editor-remaining-2026-07-29.md | 사용자 요청 "잔여 결함 이어받아 진행". 검증: 단위 749→**752**·렌더 **139**(pass 138·skip 1)·fail 0. ⑬은 **red→green 으로 세웠고**(수정 전 `1 !== 0`·`0 !== 1`) **변이 2종이 각각 대응 테스트 하나씩만** 빨간불 — rAF·`fonts.ready` 를 테스트가 직접 풀어 ⑫가 CDP 로 재현 못 한 종류의 경합을 결정적으로 재현한다. 실 Chrome 관측(`scratchpad/probe21`)으로 **과잉 취소로 측정이 죽지 않음**을 확인: 본문 삭제마다 재측정이 돌아 `warn 1→ok 0`, undo 3회 복원에서 `ok 0→warn 1`, 콘솔 오류 0. **자책 기록**: 첫 변이 실험이 CRLF 때문에 조용히 미적용돼 "초록"으로 보였다 — 문자열 `\n` 매칭 대신 줄 단위 변이기를 쓰고 적용 줄 수를 스스로 단정하게 했다 |
